package UiElements;

import org.openqa.selenium.By;

public class FileUpload  extends InputText {
    /***
     *
     * @param locator Locator strategy to find the element
     */
    public FileUpload(By locator) {
        super(locator);
    }
}
